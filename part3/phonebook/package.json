{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "phonebook server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../pbfront && npm run build --prod && cp -r build ../phonebook/",
    "deploy": "git add . && git commit -m uibuild && git push heroku master && heroku open",
    "deploy:full": "npm run build:ui && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "paul kaila",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
